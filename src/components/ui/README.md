# UI Primitives

This directory holds HeroUI-based primitive wrappers — only add a file here when you need to customise
a HeroUI component beyond its default props (e.g. a pre-styled `<Button>` variant used across many
features). If you are using a HeroUI component as-is, import it directly from `@heroui/react`.

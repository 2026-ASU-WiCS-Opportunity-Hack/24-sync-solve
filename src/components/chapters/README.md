# Chapter Components

This directory holds UI components that are specific to chapter sub-sites and are not appropriate
for global pages (e.g. chapter accent colour pickers, chapter hero variants, chapter-specific
navigation elements). Global layout components live in `../layout/`.
